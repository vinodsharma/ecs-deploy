machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t vinod/py-ecs .

test:
  post:
    - docker run --name sample-python-webapp vinod/py-ecs python main.py

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
