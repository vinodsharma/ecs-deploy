machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t vinod/go-ecs .

test:
  post:
    - docker run --name sample-go-webapp vinod/go-ecs python main.py

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
