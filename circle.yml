version: 2
jobs:
  build:
    docker:
      - image: vinodsharma/python:2.7-devel   # (1)
    # working_directory: /opt/
    steps:

      - checkout

      - setup_remote_docker   # (2)

      - run:
          name: Configure Docker Image Env Varible
          command: |
            echo 'export DOCKER_IMAGE=vinodsharma/circleci-demo-docker:$CIRCLE_SHA1' >> $BASH_ENV
